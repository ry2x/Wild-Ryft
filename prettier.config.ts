import { type Config } from 'prettier';

const config: Config = {
  tabWidth: 2,
  semi: true,
  singleQuote: true,
  printWidth: 80,
  quoteProps: 'consistent',
  arrowParens: 'always',
  trailingComma: 'none'
};

export default config;
